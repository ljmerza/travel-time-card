
export default {
  show_header: true,
  title: 'Travel Times',
  columns: ['name', 'duration', 'distance', 'route'],
  entites: [],
  map: 'google',
  distance_units: '',
};

export const validMaps = ['google', 'waze'];
export const validUnits = ['km', 'mi'];
